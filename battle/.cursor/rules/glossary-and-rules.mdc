---
description: 名词解释与游戏规则（由对话确认后更新）
alwaysApply: true
---

# 名词解释与游戏规则

本文件用于存放**已确认**的名词解释和游戏规则，便于后续对话与设计统一。  
内容应在用户明确说「写入规则」「更新名词解释」等后再更新。

---

## 名词解释

（以下为示例格式，实际条目在确认后追加或更新）

- **ATB**：Active Time Battle，主动时间战斗；单位有独立时间条，条满即可行动，顺序为「谁先满谁先动」。
- （更多术语在此追加，格式：`- **词条**：解释。`）

---

## 游戏规则（已确定）

（玩法规则、数值约定、公式约定等，确认后在此追加或更新。）

- **地牢队伍上限 `dungeon_party_max`**：单次进入地牢可编入队伍的单位人数上限；全局默认建议值见常数表（当前 4），关卡/难度可单独覆盖；进入地牢前校验「上阵人数 ≤ 上限」；影响 ATB 同场友方单位数与组队策略。

---

## 公式与数值约定

（已确定的伤害/治疗/护盾公式、参数含义、程序读取方式等。）

- **主战斗伤害（唯一主方案）**：**减法公式**——`单次有效伤害 = max(D_min, 基础伤害 − 守方防御)`。  
  - **基础伤害（进攻方）**：`攻击 × 技能倍率 × (1 ± 克制) × 上阵人数`；试算常令倍率=1、无克制。  
  - **刀数**：`⌈ 目标血量 / 单次有效伤害 ⌉`（向上取整）；目标血量为怪物单体血量，或我方「单兵血量×上阵人数」总血池等（与验算定义一致即可）。  
  - **D_min**：伤害下限，见常数表（与文档中的 `D_min` 一致；勿与「最低攻击」混淆）。  
  - **同一套攻防血统一用减法**（`max(D_min, 基础伤害 − 防御)` 或验算表约定的「血量÷攻防差」写法）；**不使用**基于 `防御/(防御+K_def)` 的减伤率公式做本项目的战斗主伤害。

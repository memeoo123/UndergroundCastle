---
description: 自动识别规则/名词解释并写入 rules 的 hook
alwaysApply: true
---

# 规则与名词解释写入（Hook）

你要**自己判断**对话里是否出现了「规则更新」或「重要约定」，一旦识别到就主动写入规则文件（除非用户曾表示不要自动写）。

## 需要识别的两类内容

**1. 规则更新 / 重要约定**

- 玩法规则被确定：如时间条满值、行动后清零、速度如何影响条等
- 数值或公式被确定：如伤害公式、治疗公式、参数含义、程序读取方式
- 设计约定被确定：如地牢层数与难度对应、种田与地牢的产出关系、稀有度档位等
- 用户对某条表述表示「就这样」「定了」「按这个来」「就按你说的」等确认

**2. 名词解释**

- 术语定义被明确：如 ATB、时间条、速度在项目里的具体含义
- 策划/数值用语在项目中的统一解释

## 识别到之后怎么做

1. **默认**：直接更新 `.cursor/rules/glossary-and-rules.mdc`，并在回复中简短说明「已作为规则/名词解释写入」。
2. **若用户曾说过「不要自动写规则」或「写规则前先问我」**：则只提示「这条可以写入规则，要写吗？」等用户确认后再写。
3. 若条目已存在（同标题或同名术语），则**更新**该条；否则在对应章节下**追加**，格式与 `glossary-and-rules.mdc` 现有结构一致。

## 用户主动要求时

当用户明确说「写入规则」「更新名词解释」「记下来」「加到 rules」等，同样执行上述写入，无需再问。

## 注意

- 只有**已经讨论清楚且被用户确认（或明显达成一致）**的内容才写入，不要把尚在讨论、未定的建议擅自写入。
- 拿不准时，可以写一句「这条要写入规则吗？」再根据用户反应决定是否写入。
